# =====================================================
# RDM Klimatizacija - Environment Variables Template
# =====================================================
# Kopiraj ovaj fajl u .env i popuni sa svojim vrijednostima
# NIKAD ne commitaj .env fajl u Git!

# =====================================================
# SITE CONFIGURATION
# =====================================================
# Samo za development - produkcijska domena je u src/config/site.ts
PUBLIC_SITE_URL=http://localhost:4321

# =====================================================
# CMS CONFIGURATION (Decap CMS / Netlify CMS)
# =====================================================
# GitHub OAuth za CMS autentifikaciju (postavlja se u Vercel/Netlify)
# Za lokalni development koristi proxy backend
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# GitHub Repository (gdje se nalazi kod)
GITHUB_REPO=webdevcodeblue/rdm-klimatizacija
GITHUB_BRANCH=main

# =====================================================
# ANALYTICS & TRACKING
# =====================================================
# Google Analytics (opciono)
PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# Vercel Analytics (automatski uključen na Vercelu)
# Ne treba konfiguracija ako je projekt na Vercelu

# Plausible Analytics (alternativa za Google Analytics)
# PUBLIC_PLAUSIBLE_DOMAIN=rmfrigoteam.hr

# =====================================================
# EMAIL CONFIGURATION (za kontakt forme)
# =====================================================
# Ako koristiš service kao SendGrid, Resend, ili Mailgun
EMAIL_SERVICE_API_KEY=your_email_service_api_key
EMAIL_FROM=info@rmfrigoteam.hr
EMAIL_TO=rmfrigoteam@gmail.com

# =====================================================
# SECURITY
# =====================================================
# Session secret (za autentifikaciju ako koristiš sessions)
SESSION_SECRET=your_random_session_secret_min_32_characters

# CSRF Token Secret
CSRF_SECRET=your_random_csrf_secret_min_32_characters

# =====================================================
# API KEYS (ako koristiš eksterne API-je)
# =====================================================
# Google Maps API (ako dodaš mapu na kontakt stranicu)
# PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# reCAPTCHA (za zaštitu kontakt forme od spama)
# PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key
# RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key

# =====================================================
# DATABASE (ako dodaš u budućnosti)
# =====================================================
# PostgreSQL, MySQL, ili drugi database
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# =====================================================
# BUILD & DEPLOYMENT
# =====================================================
# Node environment
NODE_ENV=development

# Build opcije
PUBLIC_BUILD_TIME=auto

# =====================================================
# DEVELOPMENT TOOLS
# =====================================================
# Debug mode
DEBUG=false

# Log level
LOG_LEVEL=info

# =====================================================
# NAPOMENE
# =====================================================
# 1. Svi PUBLIC_* environment variabli su dostupni u browseru
# 2. Environment variabli bez PUBLIC_ su dostupni samo na serveru
# 3. Za produkciju postavi sve variable u Vercel Dashboard:
#    Settings → Environment Variables
# 4. Za lokalni development:
#    - Kopiraj ovaj fajl: cp .env.example .env
#    - Popuni sa svojim vrijednostima
#    - .env fajl je automatski ignoriran u .gitignore
# 5. Za CMS lokalni development koristi proxy backend:
#    npx @staticcms/proxy-server
