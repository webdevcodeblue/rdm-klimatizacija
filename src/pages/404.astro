---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = 'Stranica nije pronađena';
const pageDescription = 'Tražena stranica ne postoji ili je premještena. Molimo vratite se na početnu stranicu.';
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  keywords="404, greška, stranica nije pronađena"
  noindex={true}
>
  <style>
    .error-404 {
      min-height: 60vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: var(--spacing-xl) var(--spacing-md);
    }

    .error-content {
      text-align: center;
      max-width: 600px;
    }

    .error-code {
      font-size: clamp(4rem, 10vw, 8rem);
      font-weight: 700;
      color: var(--primary-color);
      margin: 0;
      line-height: 1;
      font-family: var(--font-heading);
    }

    .error-title {
      font-size: var(--fs-2xl);
      margin: var(--spacing-md) 0;
      color: var(--text-dark);
    }

    .error-message {
      font-size: var(--fs-lg);
      color: var(--text-muted);
      margin-bottom: var(--spacing-lg);
      line-height: 1.6;
    }

    .error-actions {
      display: flex;
      gap: var(--spacing-md);
      justify-content: center;
      flex-wrap: wrap;
      margin-top: var(--spacing-xl);
    }

    .error-link {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-xs);
      padding: var(--spacing-sm) var(--spacing-lg);
      background: var(--primary-color);
      color: white;
      text-decoration: none;
      border-radius: var(--radius-md);
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .error-link:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(0, 91, 187, 0.2);
    }

    .error-link-secondary {
      background: transparent;
      color: var(--primary-color);
      border: 2px solid var(--primary-color);
    }

    .error-link-secondary:hover {
      background: var(--primary-color);
      color: white;
    }

    .error-icon {
      width: 20px;
      height: 20px;
    }

    .error-illustration {
      margin-bottom: var(--spacing-lg);
    }

    .error-illustration svg {
      width: 200px;
      height: 200px;
      opacity: 0.8;
    }

    @media (max-width: 768px) {
      .error-code {
        font-size: 4rem;
      }

      .error-title {
        font-size: var(--fs-xl);
      }

      .error-actions {
        flex-direction: column;
        align-items: center;
      }

      .error-link {
        width: 100%;
        max-width: 250px;
        justify-content: center;
      }
    }
  </style>

  <div class="error-404">
    <div class="error-content">
      <div class="error-illustration">
        <svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
          <circle cx="100" cy="100" r="90" fill="none" stroke="var(--primary-color)" stroke-width="2" opacity="0.3"/>
          <path d="M70 80 Q100 60, 130 80" fill="none" stroke="var(--primary-color)" stroke-width="3" stroke-linecap="round"/>
          <circle cx="75" cy="90" r="5" fill="var(--primary-color)"/>
          <circle cx="125" cy="90" r="5" fill="var(--primary-color)"/>
          <path d="M70 130 Q100 110, 130 130" fill="none" stroke="var(--primary-color)" stroke-width="3" stroke-linecap="round"/>
        </svg>
      </div>

      <h1 class="error-code">404</h1>
      <h2 class="error-title">Stranica nije pronađena</h2>
      <p class="error-message">
        Ups! Stranica koju tražite ne postoji ili je premještena.
        Možda je došlo do greške u tipkanju ili je link zastario.
      </p>

      <div class="error-actions">
        <a href="/" class="error-link">
          <svg class="error-icon" fill="currentColor" viewBox="0 0 20 20">
            <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z"/>
          </svg>
          Početna stranica
        </a>

        <a href="/klima-uredaji" class="error-link error-link-secondary">
          <svg class="error-icon" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 2a4 4 0 00-4 4v1H5a1 1 0 00-.994.89l-1 9A1 1 0 004 18h12a1 1 0 00.994-1.11l-1-9A1 1 0 0015 7h-1V6a4 4 0 00-4-4zm2 5V6a2 2 0 10-4 0v1h4zm-6 3a1 1 0 112 0 1 1 0 01-2 0zm7-1a1 1 0 100 2 1 1 0 000-2z" clip-rule="evenodd"/>
          </svg>
          Proizvodi
        </a>
      </div>

      <div style="margin-top: var(--spacing-xl); padding-top: var(--spacing-lg); border-top: 1px solid var(--border-color);">
        <p style="color: var(--text-muted); font-size: var(--fs-sm);">
          Trebate pomoć? <a href="/kontakt" style="color: var(--primary-color);">Kontaktirajte nas</a> ili nazovite
          <a href="tel:+38548655600" style="color: var(--primary-color);">048 655 600</a>
        </p>
      </div>
    </div>
  </div>

  <script>
    // Track 404 errors (useful for finding broken links)
    if (typeof window !== 'undefined') {
      console.log('404 Error:', {
        url: window.location.href,
        referrer: document.referrer,
        timestamp: new Date().toISOString()
      });
    }
  </script>
</BaseLayout>